// Tailwind CSS v4.0 uses CSS-first configuration
// Theme customization is done in globals.css using @layer theme
// This file can be kept for compatibility but is not required in v4.0
// Content detection is automatic - no content array needed

import type { Config } from 'tailwindcss'

const config: Config = {
  // Content detection is automatic in v4.0 - this is optional
  // Uncomment if you need to override automatic detection
  // content: [
  //   './pages/**/*.{js,ts,jsx,tsx,mdx}',
  //   './components/**/*.{js,ts,jsx,tsx,mdx}',
  //   './app/**/*.{js,ts,jsx,tsx,mdx}',
  // ],
}
export default config

